/**
 * Plasmid module
 *
 * @module plasmid
 */

const Node = require('./node')
const entries = require('./entries')
const promise = require('./promise')

module.exports = {
  Node,
  entries,
  promise
}
